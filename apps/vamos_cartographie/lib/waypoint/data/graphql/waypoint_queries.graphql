#import "/trip/data/graphql/trip_fields.graphql"
#import "waypoint_fields.graphql"

query GetWaypoints($tripId: UUID!) {
    trip(id: $tripId) {
        ...TripFields
        waypoints {
            ...WaypointFields
        }
    }
}
