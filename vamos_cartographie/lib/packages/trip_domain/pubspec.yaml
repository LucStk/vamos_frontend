name: trip_domain
description: "Trip Domain for vamos cartography"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  latlong2: ^0.9.1
  dartz: ^0.10.1
  freezed_annotation: ^3.1.0
  domain_core:
    path: ../domain_core

dev_dependencies:
  build_runner: ^2.15.0
  freezed: ^3.2.5
