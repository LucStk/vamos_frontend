name: topology_application
description: "Topology application for vamos cartographie"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  domain_core:
    path: ../domain_core
  topology_engine:
    path: ../topology_engine
  latlong2: ^0.9.1
  dartz: ^0.10.1
  freezed_annotation: ^3.1.0
