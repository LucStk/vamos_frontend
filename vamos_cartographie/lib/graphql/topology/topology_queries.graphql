#import "vertex/vertex_fields.graphql"
#import "segments/fragments/segment_fields.graphql"

query GetTopology($tripId: Int!) {
    trip(id: $tripId) {
        id
        topology {
            vertices {
                ...VertexFields
            }
            segments {
                ...SegmentFields
            }
        }
    }
}
