#import "../fields/segment_fields.graphql"

query GetSegments($tripId: UUID!) {
    trip(id: $tripId) {
        topology {
            segments {
                ...SegmentFields
            }
        }
    }
}
