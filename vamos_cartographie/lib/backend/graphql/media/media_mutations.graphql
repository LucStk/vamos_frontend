#import "image_fields.graphql"
mutation GenerateImageUploadUrl($extension: String!) {
    generateImageUploadUrl(extension: $extension) {
        fileKey
        uploadUrl
    }
}

mutation CreateImage($fileKey: String!) {
    createImage(data: { fileKey: $fileKey }) {
        ...ImageFields
    }
}
