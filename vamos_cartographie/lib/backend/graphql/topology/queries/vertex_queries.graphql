#import "../fields/vertex_fields.graphql"
query GetVertices($tripId: Int!) {
    trip(id: $tripId) {
        topology {
            vertices {
                ...VertexFields
            }
        }
    }
}
