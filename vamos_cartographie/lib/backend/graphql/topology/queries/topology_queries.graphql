#import "../fields/vertex_fields.graphql"
#import "../fields/segment_fields.graphql"

query GetTopology($tripId: UUID!) {
    trip(id: $tripId) {
        id
        topology {
            vertices {
                ...VertexFields
            }
            segments {
                ...SegmentFields
            }
        }
    }
}
