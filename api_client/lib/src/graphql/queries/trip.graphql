#import "../fragments/trip.graphql"

query GetAllTrips {
    trips {
        ...TripFields
    }
}
query GetTrip($id: Int!) {
    trip(id: $id) {
        ...TripFields
        waypoints {
            ...WaypointFields
        }
        segments {
            ...SegmentFields
        }
    }
}
