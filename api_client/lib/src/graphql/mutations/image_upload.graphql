mutation GenerateImageUploadUrl($extension: String!) {
    generateImageUploadUrl(extension: $extension) {
        fileKey
        uploadUrl
    }
}
mutation ConfirmImageUpload($fileKey: String!) {
    confirmImageUpload(fileKey: $fileKey) {
        fileKey
    }
}
