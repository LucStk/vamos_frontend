name: api_client
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.4

# Add regular dependencies here.
dependencies:
  built_value: ^8.12.6
  ferry: ^0.16.1+2
  ferry_exec: ^0.7.0
  gql: ^1.0.1
  gql_http_link: ^1.2.0
  gql_tristate_value: ^1.1.0
  path: ^1.9.0

dev_dependencies:
  build_runner: ^2.15.0

  ferry_generator2: ^0.1.0-dev.5
  lints: ^6.0.0
  test: ^1.25.6
